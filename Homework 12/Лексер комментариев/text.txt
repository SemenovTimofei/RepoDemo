#include <stdio.h>

int comment()
{
    int a = 1;
    /*
    This is an example of a comment
    Next line of comment
    An asterisk should be shown, since it's not the end of a comment:
    Asterisk? -> * <- Asterisk?
    Also 123*456
    This is the end of the first comment
    */
    a += 9;
    int b = a / 2;
    /*Some more text*/
}